Implement this playable ad as a single HTML file. Follow the implementation plan as closely as possible. 
[Attached the MD plan file + the 3 audio files]

10:51 PM




Now let me check the frontend design skill for best practices:Now I'll build the complete playable ad. This is a substantial implementation — let me construct it section by section:Here's your playable ad, Ori — a single 373KB HTML file with all three audio files embedded as base64. Here's what's implemented following the technical plan:

**Full FSM:** SHOP → CASTING → DESCENDING → PAUSED_AT_DEPTH → CATCHING → ASCENDING_FAST → SURFACE_SUMMARY → (loop ×3 → END)

**Core systems:**
- Three.js r128 scene with fisherman, hook, fishing line, fish (body+tail+eye per tier), water surface with vertex animation, seabed, 200 bubble particles, and exponential fog that deepens with descent
- Gauge pendulum with power-curve easing, gradient arc, SVG needle, depth mapping on cast
- Fish spawner with weighted rarity tiers, depth-gated filtering, and lazy visibility reveal
- Pointer input with teleport-on-down / lerp-on-move / drift-back-on-up
- Collision detection with touch radius bonus
- Upgrade shop for Max Fish and Max Depth with cost validation

**Juice per tier:** pitch-scaled catch SFX, coin burst particles (6→50), screen shake (rare+), star bursts on vfx-canvas (epic+), full-screen white flash (legendary), rarity label popups with colored glow

**Summary sequence:** staggered fish card bounces → coin value popups → rolling total counter → coin-fly arc animation to counter → counter pop

**End screen:** dark overlay fade-in with CTA button linking to your Steam page

The round 1 tutorial pill ("Drag to move the hook!") displays during the pause-at-depth phase. No round counter is ever exposed in the DOM.